# Copy to .env (never commit .env).

# Runware key: pays for the frozen learner (and the HealthBench grader) during `stbench eval`.
RUNWARE_API_KEY=

# Only needed if the training dataset on Hugging Face is gated.
HF_TOKEN=
OPENAI_API_KEY=